---
import { getCollection } from "astro:content";
import Base from "../layouts/Base.astro";
import ProjectCard from "../components/ProjectCard.astro";
import { withBase } from "../lib/paths";

const projects = (await getCollection("projects")).sort(
  (a, b) => a.data.order - b.data.order,
);
const featured = projects.filter((p) => p.data.featured);
const rest = projects.filter((p) => !p.data.featured);
---

<Base title="Christian Liyanto — Gameplay developer">
  <main id="main">
    <section id="about" class="mx-auto grid max-w-6xl gap-10 px-5 py-16 lg:grid-cols-[240px_1fr_1fr] lg:items-start lg:py-24">
        <img
          src={withBase("images/aboutme-photo.jpg")}
          alt="Christian Liyanto"
          class="aspect-square w-40 rounded-2xl object-cover object-center ring-1 ring-line sm:w-48 lg:w-full"
        />
        <div>
          <p class="text-sm uppercase tracking-[0.2em] text-muted">About</p>
          <h1 class="mt-2 font-display text-4xl text-paper sm:text-5xl">Christian Liyanto</h1>
          <div class="mt-5 space-y-4 text-justify text-muted leading-relaxed">
            <p>
              I’m a Unity Developer and Game Developer, and I graduated from Universitas Multimedia Nusantara in 2023. I started my career at Not Boring Company as an intern in 2022 and continued there as a full-time Unity Developer after graduating, where I’m still working today.
            </p>
            <p>
              My main focus is gameplay development in Unity—turning game design concepts and documents into working gameplay and technical systems. My work covers player-facing features and the systems behind them, including combat, interactions, UI, and other gameplay mechanics. I also have experience in frontend web development through the WonderBits project, working on both its browser extension and PWA.
            </p>
            <p>
              More recently, I’ve been experimenting with agentic AI and Unity’s new CLI tooling, exploring how AI agents can interact with Unity and be integrated into a structured development pipeline for building and testing games.
            </p>
          </div>
        </div>
        <div class="rounded-2xl border border-line bg-ink-2 p-6">
          <h2 class="font-display text-xl">Technical Focus</h2>
          <dl class="mt-6 space-y-5 text-sm">
            <div>
              <dt class="text-xs font-medium uppercase tracking-[0.16em] text-muted">Engine</dt>
              <dd class="mt-1.5 text-paper">Unity · C#</dd>
            </div>
            <div>
              <dt class="text-xs font-medium uppercase tracking-[0.16em] text-muted">Platforms</dt>
              <dd class="mt-1.5 text-paper">Android · Windows · WebGL </dd>
            </div>
            <div>
              <dt class="text-xs font-medium uppercase tracking-[0.16em] text-muted">Web</dt>
              <dd class="mt-1.5 text-paper">JavaScript · TypeScript · React</dd>
            </div>
            <div>
              <dt class="text-xs font-medium uppercase tracking-[0.16em] text-muted">Currently exploring</dt>
              <dd class="mt-1.5 text-paper">Agentic AI · Unity CLI · AI development pipelines</dd>
            </div>
          </dl>
        </div>
    </section>

    <section id="work" class="border-t border-line">
      <div class="mx-auto max-w-6xl px-5 py-16">
        <div class="text-center">
          <h2 class="font-sans text-3xl font-extrabold tracking-tight text-white sm:text-4xl">
            Project Showcase
          </h2>
          <div class="mx-auto mt-3 h-[3px] w-28 rounded-full bg-[#2563eb]"></div>
        </div>

        <div class="mt-12 grid gap-6 sm:grid-cols-2 xl:grid-cols-3">
          {featured.map((project) => <ProjectCard project={project} />)}
        </div>

        {
          rest.length > 0 && (
            <div class="mt-10 grid gap-6 sm:grid-cols-2 xl:grid-cols-3">
              {rest.map((project) => (
                <ProjectCard project={project} />
              ))}
            </div>
          )
        }
      </div>
    </section>

    <section id="contact" class="border-t border-line">
      <div class="mx-auto max-w-6xl px-5 py-16">
        <p class="text-sm uppercase tracking-[0.2em] text-muted">Contact</p>
        <h2 class="mt-2 font-display text-3xl">Want to look at a build together?</h2>
        <p class="mt-4 max-w-xl text-muted">
          I am available for gameplay, Unity, and client-side game work.
        </p>
        <div class="mt-8 flex flex-wrap gap-4 text-sm">
          <a
            class="rounded-full bg-accent px-5 py-2.5 font-medium text-ink hover:brightness-110"
            href="mailto:christianliyanto@gmail.com"
          >
            christianliyanto@gmail.com
          </a>
          <a
            class="rounded-full border border-line px-5 py-2.5 hover:border-muted"
            href="https://github.com/Christian-Liyanto"
            target="_blank"
            rel="noreferrer"
          >
            GitHub
          </a>
        </div>
      </div>
    </section>
  </main>
</Base>
